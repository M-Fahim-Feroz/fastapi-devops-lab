# ==============================================================================
# OPTIONAL: Local Development Overrides for Docker Compose
# ==============================================================================
# Docker Compose has built-in defaults, so this .env file is OPTIONAL.
# Only create .env if you want to override the defaults.
#
# Default values (if .env doesn't exist):
#   POSTGRES_USER=user
#   POSTGRES_PASSWORD=password
#   POSTGRES_DB=alpha
#   DATABASE_URL=postgresql://user:password@database:5432/alpha
#   REDIS_URL=redis://redis:6379/0
#
# To use custom values:
# 1. Copy this file: cp .env.example .env
# 2. Uncomment and edit the values below
# 3. Run: docker-compose up --build
# ==============================================================================

# Database Configuration (uncomment to override defaults)
# POSTGRES_USER=myuser
# POSTGRES_PASSWORD=mypassword
# POSTGRES_DB=mydb

# Connection URLs (uncomment to override defaults)
# DATABASE_URL=postgresql://myuser:mypassword@database:5432/mydb
# REDIS_URL=redis://redis:6379/0

# Weather API Key (REQUIRED - get from https://collectapi.com/)
WEATHER_API_KEY=your_weather_api_key_here

